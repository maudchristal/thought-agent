# Thought Agent uses the ITP/IMA Replicate proxy from the browser.
# No .env or Replicate token is required for normal use.
# Only needed if you run the optional local server (server.js) with your own token.
